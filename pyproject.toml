[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nrai-controller"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []
[project.scripts]
nrai_controller = "nrai_controller:main"
